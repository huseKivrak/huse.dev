{
  "name": "huse-dev-workspace",
  "private": true,
  "version": "1.0.0",
  "description": "huse.dev workspace containing the main app and butler agent",
  "scripts": {
    "dev": "cd huse-dev-app && npm run dev",
    "build": "cd huse-dev-app && npm run build",
    "start": "cd huse-dev-app && npm run start",
    "lint": "cd huse-dev-app && npm run lint"
  }
}
